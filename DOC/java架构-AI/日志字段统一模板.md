# SELFSP 日志字段统一模板

## 1. 文档定位

本文档用于统一 `SELFSP` 所有 Java 模块的日志字段、日志分类和日志模板。

目标是让日志同时服务于：

- 人工排障
- 自动 bug 定位
- 自动回归验证
- 跨模块问题关联

## 2. 日志设计原则

统一日志必须满足：

- 一次请求可追踪
- 一个失败可归类
- 一个问题可关联测试和代码
- 多模块日志可统一检索

日志内容必须优先表达业务语义和排障价值，不写无意义文本。

## 3. 强制通用字段

所有正式日志至少应具备以下通用字段：

- `timestamp`
- `level`
- `traceId`
- `logger`
- `message`

推荐扩展字段：

- `module`
- `action`
- `status`
- `durationMs`
- `operator`
- `resourceId`
- `errorCode`
- `exceptionType`

## 4. 访问日志模板

### 4.1 适用场景

适用于：

- HTTP 请求进入
- HTTP 请求完成
- 关键 RPC 或外部调用入口

### 4.2 推荐字段

- `traceId`
- `module`
- `action`
- `method`
- `path`
- `status`
- `durationMs`
- `clientIp`

### 4.3 模板示例

```text
http_access traceId={traceId} module={module} action={action} method={method} path={path} status={status} durationMs={durationMs} clientIp={clientIp}
```

## 5. 业务日志模板

### 5.1 适用场景

适用于：

- 创建业务对象
- 修改关键状态
- 删除关键数据
- 发起关键业务动作

### 5.2 推荐字段

- `traceId`
- `module`
- `action`
- `status`
- `resourceId`
- `operator`
- `summary`

### 5.3 模板示例

```text
business_event traceId={traceId} module={module} action={action} status={status} resourceId={resourceId} operator={operator} summary={summary}
```

## 6. 业务错误日志模板

### 6.1 适用场景

适用于：

- 参数错误
- 业务校验失败
- 资源不存在
- 状态不允许

### 6.2 推荐字段

- `traceId`
- `module`
- `action`
- `status`
- `errorCode`
- `errorMessage`
- `resourceId`

### 6.3 模板示例

```text
business_failed traceId={traceId} module={module} action={action} status={status} errorCode={errorCode} errorMessage={errorMessage} resourceId={resourceId}
```

## 7. 系统错误日志模板

### 7.1 适用场景

适用于：

- 未捕获异常
- 数据库异常
- 外部依赖异常
- 配置错误
- 启动失败

### 7.2 推荐字段

- `traceId`
- `module`
- `action`
- `status`
- `exceptionType`
- `errorCode`
- `message`

### 7.3 模板示例

```text
system_error traceId={traceId} module={module} action={action} status={status} exceptionType={exceptionType} errorCode={errorCode} message={message}
```

### 7.4 要求

- 系统错误日志必须保留异常堆栈
- 不允许只打印“失败了”“报错了”这类空洞信息

## 8. SQL 与数据访问日志模板

### 8.1 适用场景

适用于：

- 关键查询
- 关键写入
- 慢 SQL 排查

### 8.2 推荐字段

- `traceId`
- `module`
- `dao`
- `sqlId`
- `durationMs`
- `rowCount`
- `status`

### 8.3 模板示例

```text
dao_access traceId={traceId} module={module} dao={dao} sqlId={sqlId} status={status} durationMs={durationMs} rowCount={rowCount}
```

## 9. 启动与配置日志模板

### 9.1 适用场景

适用于：

- 应用启动
- 配置装载
- 外部依赖初始化

### 9.2 推荐字段

- `module`
- `action`
- `status`
- `configKey`
- `configValueSummary`

### 9.3 模板示例

```text
startup_event module={module} action={action} status={status} configKey={configKey} configValueSummary={configValueSummary}
```

要求：

- 配置日志只输出可公开摘要
- 不直接打印密码、token、密钥等敏感值

## 10. traceId 使用规范

要求：

- 每个请求必须有唯一 `traceId`
- 上游透传优先复用
- 下游日志统一带 `traceId`
- 响应头回写 `traceId`

作用：

- 串联 Controller、Service、DAO、异常和测试日志
- 支撑自动 bug 修复时的单次请求回放分析

## 11. 敏感信息规范

禁止直接写入日志：

- 明文密码
- token 原文
- access key / secret key
- 大段原始个人隐私信息

允许写摘要或脱敏值，例如：

- `tokenPrefix`
- `operatorId`
- `resourceId`

## 12. 自动 bug 修复友好要求

为支持后续自动 bug 修复，日志应尽量满足：

- 同一类失败拥有稳定 `errorCode`
- 关键业务动作拥有稳定 `action`
- 关键模块拥有稳定 `module`
- 日志字段顺序和语义尽量一致

这样后续才能把：

- 日志
- 测试
- 缺陷
- 修复记录

做稳定关联。

## 13. 推荐字段字典

建议统一使用：

- `module`：模块名
- `action`：业务动作
- `status`：成功/失败/进行中
- `resourceId`：业务主键或资源标识
- `operator`：操作人或调用方
- `durationMs`：耗时
- `errorCode`：稳定错误码
- `exceptionType`：异常类型
- `summary`：可读摘要

## 14. 执行要求

以后新增模块或重构模块时：

1. 访问日志、业务日志、系统错误日志必须按本文档统一。
2. 不得在正式业务代码中继续扩散 `System.out.println`。
3. 不得输出无 `traceId` 的核心请求链路日志。
4. 新增重要业务动作时，应同时补日志模板与日志字段说明。
