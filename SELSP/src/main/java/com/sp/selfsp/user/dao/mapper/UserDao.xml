<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.selfsp.user.dao.UserDao">

  <resultMap id="UserResultMap" type="com.sp.selfsp.user.domain.User">
    <id column="id" property="id"/>
    <result column="name" property="name"/>
    <result column="email" property="email"/>
    <result column="status" property="status"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <sql id="BaseColumns">
    id, name, email, status, created_at, updated_at
  </sql>

  <insert id="insert" parameterType="com.sp.selfsp.user.domain.User"
          useGeneratedKeys="true" keyProperty="id">
    INSERT INTO sys_user (
      name,
      email,
      status,
      created_at,
      updated_at
    ) VALUES (
      #{name},
      #{email},
      #{status},
      CURRENT_TIMESTAMP,
      CURRENT_TIMESTAMP
    )
  </insert>

  <update id="updateById" parameterType="com.sp.selfsp.user.domain.User">
    UPDATE sys_user
    SET
      name = #{name},
      email = #{email},
      status = #{status},
      updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>

  <delete id="deleteById">
    DELETE FROM sys_user
    WHERE id = #{id}
  </delete>

  <select id="selectById" resultMap="UserResultMap">
    SELECT
      <include refid="BaseColumns"/>
    FROM sys_user
    WHERE id = #{id}
  </select>

  <select id="selectByEmail" resultMap="UserResultMap">
    SELECT
      <include refid="BaseColumns"/>
    FROM sys_user
    WHERE email = #{email}
  </select>

  <select id="selectList" parameterType="com.sp.selfsp.user.domain.in.UserQueryIn"
          resultMap="UserResultMap">
    SELECT
      <include refid="BaseColumns"/>
    FROM sys_user
    <where>
      <if test="name != null and name != ''">
        AND name LIKE CONCAT('%', #{name}, '%')
      </if>
      <if test="status != null and status != ''">
        AND status = #{status}
      </if>
    </where>
    ORDER BY id DESC
  </select>
</mapper>
